# ── Claude Code internal files (not for version control) ──
# NOTE: README.md, docs/, and sample_docs/ are intentionally COMMITTED.
#       Only the files listed below are excluded.
/CLAUDE.md
/PLAN.md
/ERROR_LOG.md
/WORKING_LOG.md

# ── Claude Code config ──
.claude/

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
.venv/
env/
ENV/
*.egg-info/
dist/
build/
.eggs/
pip-wheel-metadata/
*.egg

# ── Environment / secrets ──
.env
.env.*
*.env
!.env.example

# ── ChromaDB data ──
chroma_data/
chroma_db/
*.chroma

# ── sentence-transformers model cache (if stored locally in project) ──
models/
.cache/

# ── Node / Frontend ──
node_modules/
frontend/dist/
frontend/build/
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── Test / Coverage ──
.pytest_cache/
.coverage
htmlcov/
coverage.xml
